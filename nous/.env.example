# wrapper-nous — production (transparent proxy)
NOUS_BASE_URL=https://inference-api.nousresearch.com
AUTH_PATH=/root/.hermes/profiles/ilma/auth.json
# NOUS_API_KEY=your-static-token

LISTEN_HOST=127.0.0.1
LISTEN_PORT=9106

# Optional seed for dynamic aliases only (operator choice — not a code hardcode).
# When unset, aliases bind to the last concrete model the client called.
# DYNAMIC_ALIAS_TARGET=tencent/hy3:free
# DEFAULT_MODEL / REASONING_MODEL are legacy optional seeds for health probe only.
DEFAULT_MODEL=tencent/hy3:free
REASONING_MODEL=tencent/hy3:free

BEARER_TOKEN=wrapper-local-key

# FREE_ONLY=yes → catalog + requests limited to ids containing "free"
# FREE_ONLY=no  → transparent (default). Does not pick models for the client.
FREE_ONLY=no
# Optional free ids that do not contain the substring "free"
FREE_MODEL_ALLOWLIST=

HEARTBEAT_INTERVAL_MS=5000
RATE_LIMIT_RPM=60
MAX_CONCURRENT_STREAMS=32

# Multi-key resilience / cooldowns
NOUS_HARD_LIMIT_RPM=60
RATE_LIMIT_COOLDOWN_SEC=65
AUTH_KEY_COOLDOWN_SEC=300
TRANSIENT_KEY_COOLDOWN_SEC=15
KEY_COOLDOWN_MAX_SEC=300
# Optional static multi-key pool (used after AUTH_PATH token, or as primary if AUTH_PATH absent)
# NOUS_API_KEY_1=
# NOUS_API_KEY_2=
# NOUS_API_KEY_3=
# NOUS_API_KEY_4=
# NOUS_API_KEY_5=
