# B-16 fix: the test suite previously had NO requirements file, so a clean CI
# runner could not even collect it (ModuleNotFoundError: dotenv / aiosqlite /
# uvicorn), and @pytest.mark.asyncio silently no-op'd because pytest-asyncio
# was never declared — async tests reported "passed" without ever running.
#
# Install with:  python -m pip install -r tests/requirements.txt

pytest>=8.0,<9
pytest-asyncio>=0.23,<1

# Runtime deps required to import the wrapper modules under test.
fastapi>=0.115,<1
starlette>=0.38
uvicorn[standard]>=0.30
aiohttp>=3.10,<4
aiosqlite>=0.20
python-dotenv>=1.0,<2
httpx>=0.27
pydantic>=2.9,<3

# MCP must stay <2 — the wrappers import mcp.server.fastmcp, which was
# restructured in 2.x.
mcp>=1.2,<2

# CODEX-RESP-02: the SDK-compat gate boots wrappers and parses their
# /v1/responses output with the official openai SDK stream parser (the exact
# code path Codex uses). Requires the SDK.
openai>=1.40,<3
