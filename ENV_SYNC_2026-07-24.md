# Env Sync Log — 2026-07-24

Purpose: record variables added to local `.env` files after diffing against
`.env.example` (post `git pull`). `.env` is gitignored (local-only, contains
secrets) so this file is the tracked artifact of that sync.

## nvidia-python (13 vars added)
DEFAULT_TEMPERATURE, DEFAULT_TOP_P, DROP_PARAMS, HARD_LIMIT_RPM,
HEADERS_TIMEOUT_MS, LOAD_SHEDDING_ENABLED, NVIDIA_BASE_URL, PACING_MAX_WAIT,
REQUEST_TIMEOUT, SOFT_LIMIT_RPM, STREAM_HEARTBEAT, WRAPPER_JSON_LOG, WRAPPER_PARAMS

## nous (15 vars added)
AUTH_KEY_COOLDOWN_SEC, AUTH_PATH, CONNECT_TIMEOUT_SEC, DEFAULT_MODEL,
FREE_MODEL_ALLOWLIST, KEY_COOLDOWN_MAX_SEC, MAX_CONNECTIONS,
MAX_CONNECTIONS_PER_HOST, NOUS_BASE_URL, NOUS_HARD_LIMIT_RPM,
RATE_LIMIT_COOLDOWN_SEC, REASONING_MODEL, REQUEST_TIMEOUT_SEC,
STREAM_REQUEST_TIMEOUT_SEC, TRANSIENT_KEY_COOLDOWN_SEC

## opencode (10 vars added)
AUTH_KEY_COOLDOWN_SEC, CONNECT_TIMEOUT_SEC, HARD_LIMIT_RPM,
KEY_COOLDOWN_MAX_SEC, MAX_CONNECTIONS, MAX_CONNECTIONS_PER_HOST,
RATE_LIMIT_COOLDOWN_SEC, REQUEST_TIMEOUT_SEC, SOFT_LIMIT_RPM,
TRANSIENT_KEY_COOLDOWN_SEC

## Notes
- API_KEY_* vars intentionally NOT added (real keys already present locally).
- All values taken from `.env.example` defaults (no secrets in this file).
- Ports unchanged: nvidia-python=9101, nous=9102, opencode=9103.
