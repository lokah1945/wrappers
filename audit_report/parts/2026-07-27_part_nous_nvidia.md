# Deep Bit-Level Audit — proxy-wrapper: `nous` + `nvidia-python`

Audit date: 2026-07-27 · Repo HEAD `21d0f79` · READ-ONLY review (no files modified, no services touched). No API key values are reproduced anywhere in this report.

Scope read in full: `nous/wrapper_nous.py` (2002 lines), `nvidia-python/src/main.py` (2646), `key_pool.py` (949), `anthropic_compat.py` (1130), `responses_compat.py` (685), `capabilities.py` (skimmed, cache paths verified), `registry.py`, `metrics.py`, `loki_push.py`, `alert_history.py`; `.env` key *names* only; log tails; shared `common/` modules referenced by both.

---

## 0. Recent-Fix Regression Verification

| Commit | Claimed fix | Verdict |
|---|---|---|
| `f5bfeea` (Codex hang, nous) | ① Heartbeat fires while upstream idle — **PRESENT**: `wrapper_nous.py:1044-1061` uses `asyncio.wait_for(chunk_iter.__anext__(), timeout=HEARTBEAT_MS/1000)` and yields `: heartbeat` on timeout. ② `store_conversation` moved to the generator's `finally` so stream finalization isn't blocked — **PRESENT**: `wrapper_nous.py:1881-1889`. ③ Terminal `response.completed` emitted exactly once (`ResponsesStreamState.done()` idempotent, `_completed` flag) — **PRESENT**: 1328-1357. | **Fix present**, but two residual issues remain in the same code: TimeoutError conflation (N-05) and yields inside `finally` on client disconnect (N-07). `store_conversation`'s backing store is still unbounded (N-02). |
| `68c1d47` (nvidia sanitize NameError) | `sanitize_header_value` imported from `common.middleware` with inline fallback — **PRESENT**: `main.py:61-76`; used in `forward_headers` `main.py:1080`. | **Fix present and complete.** |
| `21d0f79` (cross-wrapper sanitize NameError) | nous imports `_sanitize_header_value` with `sys.path` bootstrap + inline fallback — **PRESENT**: `wrapper_nous.py:57-70`; used at 1826, 1864, 1931. Verified `common/middleware.py:88` defines `sanitize_header_value`. | **Fix present and complete.** |

Sibling-pattern findings from these fixes are marked ⚠SIBLING in the tables (e.g., the SEC3 max-tokens cap and the bounded-store pattern were each applied to only *some* endpoints/components).

---

## 1. Component: `nous` (`/root/wrapper/nous/wrapper_nous.py`)

### Findings

| ID | Sev | Location | Description | Failure scenario | Suggested fix (do not apply) |
|---|---|---|---|---|---|
| N-01 | **HIGH** | `wrapper_nous.py:554-588`, `648-677` | `post_nous()` has no try/except around `sess.post(...)`. Any `aiohttp` connection error (DNS fail, conn reset, `ClientConnectorError`, timeout raised as non-`asyncio.TimeoutError`) propagates out of `post_nous_with_retries` **after `KEY_POOL.acquire()` incremented `in_flight`** (line 663) — the key entry is never released. | Upstream outage/network blip → 500 to client *and* permanent `in_flight` leak on the key. `effective_load` inflates forever (nous `KeyPool` has no `heal_in_flight`), skewing least-loaded rotation until, over time, rotation fairness collapses onto other keys. | Wrap the `post_nous` call in `try/except Exception`, convert to `(502, {...})`, and `KEY_POOL.release(entry)` in a `finally`. Add a heal routine like nvidia's. |
| N-02 | **HIGH** | `wrapper_nous.py:829-834, 776-777, 1887, 1904` | `_RESPONSE_STORE` (Codex `previous_response_id` history) grows unboundedly: `store_conversation` inserts full conversations (messages incl. tool outputs) per response id and there is **no TTL, no cap, no eviction**. ⚠SIBLING: nvidia's `responses_compat._bounded_store` caps at 200 — the fix was not ported to nous. | Long-running Codex sessions (its primary consumer per CONTEXT) leak memory monotonically; multi-day uptime → OOM / swap death of the proxy. | Port the nvidia pattern: cap entries (e.g., 200, FIFO evict) and/or TTL-prune using the stored timestamp (which is already saved but never read). |
| N-03 | **HIGH** | `wrapper_nous.py:1974-1990` | `/dashboard` is served **without `_auth_check`** and injects the raw `BEARER_TOKEN` into the HTML `<meta>` tag. Any client that can reach the port obtains the proxy auth token. ⚠SIBLING: identical pattern in nvidia (V-06). Mitigated today only by `LISTEN_HOST=127.0.0.1` in `.env`. | Any local process/user (or anyone if bind ever changes to 0.0.0.0 / gets port-forwarded) fetches `/dashboard` and extracts the bearer token → full authenticated proxy access. | Require auth on `/dashboard`, or stop injecting the token (use a session cookie/short-lived token), or inject only when the request already presented valid auth. |
| N-04 | MED | `wrapper_nous.py:1854, 1911` | `/v1/responses` and `/v1/messages` call `await request.json()` with no try/except (unlike `/v1/chat/completions:1780-1782`). Malformed JSON → unhandled exception → generic 500 instead of a 400 `invalid_request_error`. | Client sends truncated body (common on retries/disconnects) → 500s pollute error classification; SDKs may retry 500s, amplifying load. | Same try/except + 400 envelope as `chat_completions`. |
| N-05 | MED | `wrapper_nous.py:1050-1061` | `except asyncio.TimeoutError: continue` cannot distinguish the intentional `wait_for` heartbeat timeout from a **genuine aiohttp read/total timeout raised by `chunk_iter.__anext__()`** (same exception type). A real upstream timeout is swallowed at least once and treated as "idle, keep heartbeating"; behavior then depends on aiohttp internals (usually EOF → clean finalize, but the timeout cause is lost and the stream can idle extra heartbeat cycles). | Upstream stall at the 900s total timeout: client sees heartbeats then a silent synthetic `done()` with no error indication; Codex believes the turn completed normally with truncated output. | Wrap only `wait_for` distinctly, e.g. use `asyncio.timeout()`/shielded task so upstream `TimeoutError` propagates, or compare against a sentinel task rather than exception type. |
| N-06 | MED | `wrapper_nous.py:1651-1654` | Session timeout is `ClientTimeout(total=max(600,900)=900s, sock_connect=30)` — the **total** timeout covers the entire streamed body with no `sock_read`. (a) Generations longer than 15 min are hard-killed mid-stream; (b) a silently dead upstream connection is only detected after up to 900s (no read-idle timeout), during which the wrapper emits heartbeats keeping the client waiting. | Long reasoning generation killed at 900s; or dead upstream keeps a Codex turn "alive" for 15 min before the synthetic terminal fires. | Use `sock_read` (idle) timeout for streams (e.g., 120–300s) instead of/alongside a large `total`; make streaming `total=None`. |
| N-07 | MED | `wrapper_nous.py:1082-1105`, `1874-1889` | On client disconnect, `GeneratorExit` is delivered at a `yield`; both `stream_with_heartbeat`'s `finally` (which `yield`s tail flush / done events / `[DONE]`) and the `/v1/responses` `gen()` `finally` (which `await`s `store_conversation` — can suspend on `_STORE_LOCK`) violate async-generator finalization rules → `RuntimeError: async generator ignored GeneratorExit`. Key/response release still executes (inner `finally`), but store_conversation may be skipped and errors logged every disconnect. | User hits Ctrl-C in Codex mid-stream → noisy RuntimeErrors; conversation not stored → next turn's `previous_response_id` misses assistant tool_calls → orphan `role:tool` → upstream 400 → agent stops (the exact class of bug f5bfeea targeted). | Catch `GeneratorExit`/`asyncio.CancelledError` explicitly: skip all yields, do cleanup only; persist the conversation via `asyncio.create_task`/`shield` instead of awaiting in `finally`. |
| N-08 | MED | `wrapper_nous.py:1067-1076` | SSE block splitting uses only `b"\n\n"`. A CRLF upstream (`\r\n\r\n`) never matches, so the **entire response accumulates in `buffer`** and is only parsed in the EOF flush — stream silently degrades to non-streaming with unbounded per-request buffering. | If Nous (or a proxy in front of it) ever emits CRLF SSE: no incremental deltas (Codex sees heartbeats only, then everything at EOF), plus large memory spike per request. | Split on `re` `\r?\n\r?\n`, or normalize `\r\n`→`\n` before splitting (line-level `.strip()` already tolerates `\r`). |
| N-09 | MED | `wrapper_nous.py:1020-1027` | `handle_payload` treats **empty** `data:` payload (`b""`) as `[DONE]` and emits full terminal events + terminates. Some servers send empty `data:` keep-alives; per SSE spec an empty data line is a valid (empty) event, not end-of-stream. | Upstream keep-alive `data:` line → nous prematurely fabricates `message_stop`/`response.completed` mid-generation → truncated answer presented as complete. | Only terminate on literal `[DONE]`; ignore empty payloads. |
| N-10 | MED | `wrapper_nous.py:658-677, 710-736` | Retry loop runs `total_keys` iterations, but `acquire()` re-selects by least-load — when a failure is retriable but **not** cooled down (`_should_cooldown_key` false: model-capacity 429/404), the *same key* can be re-acquired every iteration, and there is **no backoff** between attempts. | A model with "no deployments available" → N immediate back-to-back upstream hits on one key per client request; with agent retry loops on top, this is a retry storm against a down model. | Track tried labels per request and skip them; add small jittered sleep between attempts. |
| N-11 | MED | `wrapper_nous.py:1852-1866 (`/v1/responses`), 1908-1933 (`/v1/messages`)` | ⚠SIBLING gap of BUG-SEC3 (`1ad8845`) and of per-IP rate limiting: the 1,000,000 `max_tokens` cap and `check_rate_limit` are applied **only** in `/v1/chat/completions`. `/v1/messages` validates positivity but no upper cap; `/v1/responses` does `int(body["max_output_tokens"])` with neither cap **nor** type guard (`int("abc")` → unhandled 500). | Claude Code/Codex surfaces accept absurd `max_tokens` (upstream abuse / overflow class SEC3 was fixing); non-integer `max_output_tokens` crashes to 500; no per-IP throttle on the two busiest endpoints. | Apply the same cap+validation helper and `check_rate_limit` to all three inference endpoints. |
| N-12 | MED | `wrapper_nous.py:204-212` | `mark_failure` cooldowns the **whole key** for any `>=500`/408/409 (15s) and 429 (65s) even when the error is model-specific (heuristic `_looks_model_capacity_error` catches only a few phrasings). With few keys, one persistently-broken model can serially cool down every key. | Agent hammers one broken model → all keys enter cooldown → `acquire()` returns None → *all models* 503 "No capacity" for healthy traffic. | Add per-model blocks (as nvidia's pool has) instead of key-wide cooldown for 5xx tied to a specific model. |
| N-13 | MED | `wrapper_nous.py:250-277, 291-305` | Hot-reload watcher calls `load_dotenv()` which uses `os.environ.setdefault` — existing vars are **never overwritten**, so ".env reloaded (hot)" is a no-op for changed values (key rotation via .env edit does nothing until restart; also `KEY_POOL.load_from_env()` is not re-invoked). ⚠SIBLING: nvidia correctly uses `load_dotenv(override=True)`. | Operator rotates a leaked/expired key in `.env`, sees the "reloaded" log, but the wrapper keeps using the old key until restart. | On watcher event: parse with override semantics and call `KEY_POOL.load_from_env()`. |
| N-14 | LOW | `wrapper_nous.py:1465-1476` | `rate_limits` defaultdict keyed by client IP is pruned per-key but **keys are never deleted** → slow unbounded growth of dict entries (bounded in practice by 127.0.0.1 bind; real issue if exposed). Same pattern nvidia (V-13) where the key is a *spoofable* header. | Long uptime with many distinct client addresses → creeping memory. | Delete empty lists during the sweep. |
| N-15 | LOW | `wrapper_nous.py:49-56, 554-560` | Circuit breaker is inert: only `before_request()` is ever called; `record_failure`/`record_success` never — the breaker can never open. Dead code presenting false safety ("Circuit breaker for upstream protection"). | None functional; misleading during incident response. | Record success/failure around `post_nous`, or remove. |
| N-16 | LOW | `wrapper_nous.py:1581-1596` | `/ready` performs a live upstream `/v1/models` call (through key pool with retries) on **every** invocation, unauthenticated. Frequent k8s/systemd probes consume key RPM and can trip upstream limits. | Aggressive health probing burns pool capacity; a local unauthenticated caller can use `/ready` as an upstream request amplifier. | Serve readiness from the cached catalog age (`MODEL_STORE`), rate-limit live probes. |
| N-17 | LOW | `wrapper_nous.py:1656-1739` | `_known_models` is only (re)populated inside the `/v1/models` handler; until a client lists models, `set_dynamic_alias_target` rejects all non-forced bindings, and the global is swapped non-atomically with a transient empty set during rebuild. | After restart, alias binding silently ignored (debug-level log only) until a discovery call happens; brief window where a concurrent request sees an empty `_known_models`. | Populate at startup from `MODEL_STORE.get_catalog`; build a new set then assign. |
| N-18 | LOW | `wrapper_nous.py:1769-1773, 1953-1971` | `/v1/messages/count_tokens`, `/metrics`, `/metrics/model-status`, `/v1/models`, `/v1/capabilities` skip `_auth_check` entirely (in nvidia these are gated by an explicit public-path allowlist middleware; nous simply never checks). No key material leaks (labels only), but usage/model telemetry is exposed. | Any local process reads traffic stats and model states without the bearer token. | Add `_auth_check` to non-discovery endpoints or adopt the middleware allowlist pattern. |
| N-19 | LOW | `wrapper_nous.py:830, 1904` | `_STORE_LOCK` guards writes, but `responses_to_chat` (line 776) **reads** `_RESPONSE_STORE[prev]` without the lock and shares the stored list objects (`msgs.extend(...)`) — subsequent in-place mutation of message dicts aliases stored history. Single event loop makes torn reads unlikely; aliasing is real. | A later turn's normalization mutates dicts that are also stored history → corrupted replay for the next `previous_response_id`. | Store/read deep copies; read under the lock. |
| N-20 | LOW | `wrapper_nous.py:1836-1848` | `metrics.record()` is called only in `chat_completions`; `/v1/messages` and `/v1/responses` (the two surfaces agents actually use) record nothing — `/metrics` materially under-reports and `error_rate` is wrong. | Dashboards show near-zero traffic while Claude Code hammers `/v1/messages` (log tail confirms heavy `/v1/messages` traffic). | Record in all handlers, including streamed completions. |
| N-21 | INFO | `wrapper_nous.py:1096-1099` vs `1025-1026` | Terminal-`[DONE]` gating uses fragile `__class__.__name__` string comparison, and the two sites use *different* conditions (`== "ResponsesStreamState"` vs `!= "AnthropicStreamState"`). Currently equivalent; any new state class will silently take different branches. | Future state machine gets `[DONE]` in one path but not the other → protocol violation for that surface. | Use an explicit class attribute, e.g. `emit_done_marker: bool`. |
| N-22 | INFO | `nous/model_catalog_template.json:1-10` | Template `slug` is `nvidia/nemotron-3-ultra-550b-a55b:free` with display name "Tencent Hunyuan hy3 (Free)" — mismatched copy-paste; every enriched model inherits fields from this entry (`_load_model_info_template` takes `models[0]`). Cosmetic (id/slug are overwritten) but confusing metadata. | Clients see stale template fields (context window 128000 etc.) for all models. | Correct the template entry. |
| N-23 | INFO | `wrapper_nous.py:1632-1643` | `get_token()` is dead code (never called); OAuth path duplicated by `_read_token_from_auth_path`. Blocking file read `_read_token_from_auth_path` executes on the event loop per request (`/health`, every retry chain) — tiny local file, acceptable, but it is sync I/O in async context. | Micro-stalls under heavy concurrency if AUTH_PATH is on slow storage. | Cache token with short TTL; drop dead code. |
| N-24 | INFO | `wrapper_nous.log` (tail) | No error evidence in recent log tail — steady 200s on `/v1/messages` — consistent with fixes being live. | — | — |

---

## 2. Component: `nvidia-python` (`/root/wrapper/nvidia-python/src/`)

### Findings

| ID | Sev | Location | Description | Failure scenario | Suggested fix (do not apply) |
|---|---|---|---|---|---|
| V-01 | **CRITICAL** | `key_pool.py:31-53` | Hand-rolled `Mutex` is not cancellation-safe. `release()` pops the head waiter; if that waiter's future was **cancelled** (client disconnected / request task cancelled while awaiting `_lock.acquire()` in `_acquire_slot`, `register_rate_limit`, `sync_keys`, …), the `if not fut.done()` guard skips `set_result` **and nothing else is woken and `_locked` stays True** → the mutex is permanently locked. | One cancelled request while queued on the pool lock → every subsequent `acquire()`/`register_rate_limit()` awaits forever → all inference endpoints hang (503-free, just hangs) until process restart. This is a realistic event: Codex/Claude Code abort requests routinely. | Replace with `asyncio.Lock` (cancellation-safe), or in `release()` loop-pop until a non-done future is found, else set `_locked=False`; in `acquire()`, on `CancelledError` remove own future from `_queue`. |
| V-02 | **HIGH** | `key_pool.py:928-933` | `prune_stale_entries`: `key_label, model = km.split('/')` — model IDs contain `/` (`key1/meta/llama-3.1-8b-instruct`) → **`ValueError: too many values to unpack` (confirmed by execution)** on the first learned per-key model limit. The exception aborts pruning every cycle (caught+logged in `_refresh_loop`). Consequences: `_key_model_limit` is never pruned (learned per-key model limits persist forever → keys permanently treated as saturated for a model after one 429), and `_model_limit` pruning for later iterations is skipped. | A single per-key-model 429 → that key is skipped for that model *forever* (saturation check at `key_pool.py:507-513, 534-537` uses the never-expiring limit); with all keys once-429'd on a model, requests for it perpetually shed/wait. Refresh loop logs an error every 600s. | `km.split('/', 1)`. |
| V-03 | **HIGH** | `key_pool.py:597-598` + `main.py:2152-2158` | `_acquire_slot` deletes the waiting ticket only on normal exit; **`CancelledError`** during `asyncio.sleep`/`signal.wait` (client disconnect while queued for pacing) propagates past `del self._waiting[my_ticket]` → ghost tickets accumulate in `_waiting`. `max_queue_size=500` backpressure then counts ghosts. Additionally ghost tickets with lower ids permanently inflate `rank`, starving later requests of pacing slots. | Bursty agents that cancel requests → `_waiting` fills with ghosts → "Queue backpressure load shed" rejects all traffic; pacing rank math starves real waiters even earlier. | `try/finally` around the wait loop to always delete `my_ticket`. |
| V-04 | **HIGH** (observed) | `loki_push.py:29-97` + `nvidia_py.log` | Loki pushes fail with **HTTP 401 in an endless loop (live evidence in log tail)**. On non-2xx the batch is *not* trimmed (`_batch` only shrinks on success) → `_batch` grows unboundedly while the daemon retries every 0.5–5s forever → memory leak + log spam. No auth header support beyond tenant id; no failure cap. | Confirmed in production log: continuous `[loki_push] HTTP 401`. Over days: unbounded `_batch` growth and log-file bloat drowning real errors. | Cap `_batch` (drop-oldest), back off exponentially on persistent 4xx, and treat 4xx as permanent (drop batch). Fix or disable the Loki credentials. |
| V-05 | **HIGH** | `main.py:2386-2392` (`_stream_proxy`) used by `_proxy_post:2340` and `_handle_catch_all:2485` | `_stream_proxy` releases the key but **never releases/closes the aiohttp response**. If the client disconnects before the stream is exhausted, the upstream connection is left un-released. ⚠SIBLING: `proxy_openai`'s `stream_wrapper` (main.py:2209-2226) *does* call `resp.release()` — the fix wasn't mirrored. | Repeated client aborts on `/v1/embeddings`, `/v1/ranking`, images, catch-all streaming → connector slots leak until `MAX_CONNECTIONS` (200) exhausted → all upstream calls stall. Also `_in_flight` is decremented here but the pool in-flight decrement relies on `release_success` only (ok), while metrics `streaming` under-counts. | Mirror `stream_wrapper`: `finally: resp.release()` (guarded), then key/counter release. |
| V-06 | **HIGH** | `main.py:1651-1680` + middleware `main.py:1315` | `/dashboard` and `/dashboard.html` are in `public_paths` (no auth) yet `_serve_dashboard_html` injects the raw `BEARER_TOKEN` into the served HTML. Same issue as N-03; explicitly documented in the docstring as intended behavior — it is an auth bypass by construction. Mitigated only by `.env` `LISTEN_HOST=127.0.0.1` (note code default is `0.0.0.0`, `main.py:559` — a deleted env line silently exposes it). | Anyone who can reach the port reads the bearer token from an unauthenticated page → full access incl. destructive `POST /metrics/reset`, `/admin/heal-in-flight`. | Gate token injection on an authenticated request, or drop injection and require manual token entry. Change code default bind to 127.0.0.1. |
| V-07 | MED | `main.py:2495-2498` (`_handle_catch_all`) | Unlike `proxy_openai`/`_proxy_post`, the catch-all retries **every** `>=400` (including deterministic 400/404/422) across `max(MAX_RETRIES+1, total_keys)` keys with no classification and no backoff. | One malformed legacy/Ollama-path request → up to N identical upstream calls, burning per-key RPM (each `acquire()` also records a timestamp toward the RPM window) and delaying the client's error by the full retry chain. | Reuse `classify_upstream_error` + `_classify_retry` as the other paths do. |
| V-08 | MED | `main.py:288-296, 1089-1093, 1330-1332` | Per-IP rate limiting keys on `client_ip()` which trusts `x-forwarded-for` (fully client-controlled — the wrapper is a directly-reached local service, not behind a trusted LB). (a) Bypass: rotate XFF values to dodge the limit; (b) poisoning: set XFF to a victim string; (c) memory: `_rate_limit_store` keys are never deleted → unbounded growth driven by attacker-chosen strings. | Local multi-tenant box or accidental exposure: rate limit is decorative and the store grows without bound. | Use the socket peer address (`request.client.host`); prune empty keys. |
| V-09 | MED | `main.py:571-583, 2052-2059` | Streaming timeout = `max(STREAM_REQUEST_TIMEOUT_SEC=600, ANTI_SILENCE_TIMEOUT_MS/1000=960)` as an aiohttp **total** timeout: long generations are killed at 16 min mid-stream; and there is no read-idle timeout, so a dead upstream holds the stream open (there's no proxy-side heartbeat on the `/v1/chat/completions` passthrough either — `_stream_chat` forwards raw chunks only; Claude Code path relies on upstream cadence). `TTFT_TIMEOUT_MS`/`PRE_RESPONSE_TIMEOUT_MS` are loaded but never enforced on the proxy path (`pre_response_timeout_ms_for` unused). | Very long agent generations truncated at 960s; silent upstream → client hangs until total timeout with no anti-silence signal despite env `ANTI_SILENCE_TIMEOUT_MS` implying protection. | Use `sock_read` timeout + heartbeat comments for chat SSE (as nous does); enforce TTFT separately. |
| V-10 | MED | `key_pool.py:187-214` (`stats()` → `/stats`, `/metrics`, `/metrics/keys`) | `key_prefix: api_key[:16] + '...'` — 16 characters of each live API key are exposed via metrics endpoints (bearer-protected, but also rendered into the dashboard and copied into any metrics scrape/log pipeline). Combined with V-06 this becomes unauthenticated partial key disclosure. | Key prefixes leak into dashboards/scrapes/incident pastes; materially narrows brute-force / identifies accounts. | Truncate to ≤6 chars or replace with `credential_fingerprint()` hash. |
| V-11 | MED | `main.py:1194-1205` | Per-request event write: `asyncio.create_task(_write_event(ev))` performs **blocking** `open/write` on the event loop for every request and rate-limit event, and `wrapper-events.jsonl` has no rotation/size cap (alert_history/loki only read it). | High RPS → event-loop stalls from sync disk I/O; disk fills over weeks. | Use a thread (`asyncio.to_thread`) or queue+single writer; rotate the file. |
| V-12 | MED | `main.py:2179-2180, 2235-2236, 2322, 2466` | `ra = int(resp.headers.get('retry-after', '65') or '65')` — an HTTP-date `Retry-After` (allowed by RFC) raises `ValueError`, which falls into the broad `except Exception` → decrements happen, but the 429 is **not registered** with the pool (no cooldown, no classification) and the same key is immediately re-acquirable. | Upstream that sends date-format Retry-After → cooldown machinery silently disabled → tight 429 retry loop across attempts. | Parse int, fall back to date parsing, default 65. |
| V-13 | MED | `main.py:246-253` (`verify_loop`) + `probe_model:116-150` | Verify sweep probes up to 100 models × distinct-credential keys every 10 min *outside* pool accounting (`peek_key`/direct key use, no `acquire`) — probe traffic doesn't count toward per-key RPM tracking, so the pool's view of key load is wrong right after each sweep, and probes can themselves trigger real 429s that then hit client traffic. `VERIFY_ON_BOOT` parse (`main.py:580`) is inverted-ish: any value other than exactly `'false'` (e.g. `no`, `0`) enables it. | Sweep bursts consume hidden RPM → client requests hit unexpected upstream 429s; operator sets `VERIFY_ON_BOOT=no` and gets a boot-time probe storm anyway. | Route probes through `pool.acquire` or record timestamps on the key; parse boolean with a truthy-set. |
| V-14 | MED | `main.py:1731-1750` | `/v1/responses` handler runs a full recursive `_walk` over the entire request body on **every** request purely to build a `logger.debug` string (debug logging normally disabled, but the walk itself always executes). O(body) CPU per request on the Codex hot path; large tool-heavy histories are frequent. | Measurable latency/CPU overhead on the busiest endpoint; giant payloads amplify it. | Guard with `logger.isEnabledFor(logging.DEBUG)` or delete the debug scaffold. |
| V-15 | LOW | `main.py:2015-2032` + `anthropic_compat.py:996-1026` | On client disconnect, `stream_openai_to_anthropic`'s `finally` yields SSE events (`content_block_stop`, tool flush) → `RuntimeError: async generator ignored GeneratorExit` (same pattern as N-07). Upstream release *is* attempted first (good). The outer `anthropic_stream` wrapper also yields `message_stop` from its `except` — safe for upstream errors, wrong-but-harmless for cancellation. | Noisy RuntimeErrors on every mid-stream abort by Claude Code; masked real errors in logs. | Detect `GeneratorExit`/`CancelledError` and skip terminal yields. |
| V-16 | LOW | `responses_compat.py:584-587` | Final partial SSE line (no trailing newline) is deliberately ignored — a last-chunk `usage` or terminal delta is dropped (documented tradeoff). Also multi-line `data:` events are not concatenated per SSE spec (each line parsed independently) — same in nous. | Usage under-reporting; nonconforming (but rare) upstream framing loses a delta. | Parse the buffered tail like a normal line before finalization. |
| V-17 | LOW | `main.py:1209-1210, 1263-1264` + `registry.py:149-158`, `metrics.py:126` | Background tasks (`alert_history.mode_daemon`, `loki_push.daemon`, `verify_loop`, `start_model_refresh`, `prune_dedupe`) are created with bare `create_task` and never retained/cancelled at shutdown (only registry timer and metrics saver are). Also `Metrics.prune()` exists but is **never scheduled** → `metrics.db` `requests` table grows forever (every request inserted, pruned only manually). | Shutdown warnings/leaked tasks; more importantly the SQLite DB grows unboundedly, slowing `summary()` percentile OFFSET scans over months. | Keep task handles, cancel in lifespan; schedule `prune()` daily. |
| V-18 | LOW | `main.py:2161, 2179, 2221…` (`self._in_flight`), `key_pool.py` counters | `Server._in_flight` and key counters are mutated from async code without locks — safe on one event loop, but `heal_in_flight` exists precisely because leaks happen and it is only reachable via manual `POST /admin/heal-in-flight`; the Node parity periodic heal was not scheduled. | Stuck `in_flight` after rare double-decrement/exception interleavings persists until an operator remembers the admin endpoint; load shedding (`INFLIGHT_SOFT_CAP`) can then falsely reject traffic. | Schedule `heal_in_flight()` periodically (e.g. every 5 min). |
| V-19 | LOW | `main.py:1334-1349` | Auth: token comparison `token != BEARER_TOKEN` is not constant-time (both wrappers); `authorization` header without `Bearer ` prefix is accepted as the raw token (lenient, fine), but when a garbage `authorization` header is present a valid `x-api-key` is ignored (`auth_header or api_key_header` short-circuits). | Clients sending both headers (some SDKs do) get 401 despite a valid x-api-key. | Compare with `hmac.compare_digest`; check both credentials independently. |
| V-20 | LOW | `main.py:227-244` (`verify_models`) | `observations` dict mutated by up to `VERIFY_CONCURRENCY` coroutines — safe (single loop), but `latest = max(results, key=account_scope)` selects the "latest" status by **alphabetical fingerprint**, not recency; `_model_status[mid]` may reflect a stale/arbitrary account. | `/metrics/model-status` shows the wrong per-model state when accounts disagree. | Track and sort by probe timestamp. |
| V-21 | INFO | `main.py:2624-2628` | Post-hoc override `AnthropicStreamState = _SharedAnthropicStreamState` after all class definitions — the 120-line local class (main.py:377-498) is dead weight whenever `common.translations` imports; divergence risk if the fallback is ever exercised. | Fallback path silently uses an older state machine with different semantics. | Delete local copy or add a parity test. |
| V-22 | INFO | `responses_compat.py:46-51` | `_bounded_store` cap of 200 conversations is present (good sibling of N-02 fix) but eviction pops arbitrary-ish first key (insertion order = oldest, OK) and stores full message lists — with giant Codex histories 200 entries can still be hundreds of MB. | Heavy Codex use with huge contexts → large steady-state memory. | Cap by total bytes as well, or store only the delta needed for orphan-tool repair. |
| V-23 | INFO | `metrics.py:391-402` | Percentile query uses `ORDER BY latency_ms LIMIT 1 OFFSET n` — O(n log n) per call, twice per `/health` (which also runs `summary('24h')`). Frequent health polling makes `/health` progressively expensive as the table grows (see V-17 pruning gap). | `/health` latency creeps up over weeks. | Add covering index / cache summary briefly. |

---

## 3. Cross-Component Summary (highest-priority actions)

1. **V-01 (CRITICAL)** — replace the hand-rolled `Mutex` in `key_pool.py` with `asyncio.Lock`; a single cancelled request can deadlock the entire nvidia key pool forever.
2. **V-02 / V-03 (HIGH)** — `split('/', 1)` fix and ticket-cleanup `finally`; both silently degrade key rotation and eventually shed all traffic.
3. **N-01 (HIGH)** — wrap `post_nous` network errors; current behavior leaks key in-flight slots permanently and 500s clients on any upstream connection error.
4. **N-02 (HIGH, sibling of an already-fixed nvidia bug)** — bound `_RESPONSE_STORE` in nous exactly as `responses_compat._bounded_store` does.
5. **N-03 / V-06 (HIGH)** — stop serving the bearer token in unauthenticated `/dashboard` HTML in both wrappers; also change nvidia's code-default bind from `0.0.0.0`.
6. **V-04 (HIGH, live)** — loki 401 retry storm is happening right now per `nvidia_py.log`; bound the batch and fix/disable Loki auth.
7. **V-05 (HIGH)** — add `resp.release()` to `_stream_proxy` (the fix already exists in `stream_wrapper`; sibling instance missed).
8. **Sibling-gap sweep** — SEC3 max-tokens cap and per-IP rate limiting missing on nous `/v1/messages` + `/v1/responses` (N-11); `.env` hot-reload override semantics missing in nous (N-13); GeneratorExit-safe stream finalization missing in both (N-07 / V-15).

f5bfeea, 68c1d47, and 21d0f79 are all verified present and functionally complete; the residual risks around them are the sibling patterns listed above, not regressions of the fixes themselves.